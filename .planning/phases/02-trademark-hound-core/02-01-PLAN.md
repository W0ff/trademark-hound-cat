---
phase: 02-trademark-hound-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase2.py
autonomous: true
requirements:
  - HND-01
  - HND-02
  - HND-03
  - HND-04
  - HND-05
  - HND-07
  - HND-08
  - HND-09
  - HND-10
  - HND-11
  - HND-12

must_haves:
  truths:
    - "tests/test_phase2.py exists and runs without import errors"
    - "All HND-01 through HND-12 tests fail RED (contract not yet satisfied)"
    - "python3 tests/test_phase1.py still passes GREEN (no regressions)"
  artifacts:
    - path: "tests/test_phase2.py"
      provides: "Contract test suite for .claude/commands/trademark-hound.md"
      exports: ["TestTrademarkHoundIntake", "TestTrademarkHoundSERP", "TestTrademarkHoundScoring"]
  key_links:
    - from: "tests/test_phase2.py"
      to: ".claude/commands/trademark-hound.md"
      via: "Path existence check + assertIn structural checks"
      pattern: "HOUND_CMD.exists()"
---

<objective>
Create the Wave 0 test scaffold for Phase 2. Write tests/test_phase2.py containing contract tests for .claude/commands/trademark-hound.md covering HND-01 through HND-12 (minus HND-06, already covered by tests/test_phase1.py::TestHoundLeadsTemplate::test_py02_delay_seconds). Tests must be committed RED — the command file does not exist yet, so all tests should fail on the file-existence check or structural assertions.

Purpose: Nyquist compliance — tests written before implementation prove they check real artifacts.
Output: tests/test_phase2.py with ~12 test methods across 3 test classes, all failing RED.
</objective>

<execution_context>
@/Users/woff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/woff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/test_phase1.py
@.planning/phases/02-trademark-hound-core/02-VALIDATION.md
</context>

<interfaces>
<!-- Established test pattern from Phase 1. Executor must follow this exactly. -->

From tests/test_phase1.py (pattern to replicate):
```python
import os
import ast
import unittest
from pathlib import Path

ROOT = Path(__file__).parent.parent

class TestTrademarkCatCommand(unittest.TestCase):
    CAT_CMD = ROOT / ".claude" / "commands" / "trademark-cat.md"

    def _read(self):
        self.assertTrue(self.CAT_CMD.exists(), f"Missing: {self.CAT_CMD}")
        return self.CAT_CMD.read_text(encoding="utf-8")

    def test_cat01_file_exists(self):
        """CAT-01: Command file exists"""
        self.assertTrue(self.CAT_CMD.exists(), f"Missing: {self.CAT_CMD}")
```

Key decisions already locked:
- Framework: Python stdlib unittest only (no pytest)
- Run command: python3 tests/test_phase2.py 2>&1 | tail -3
- HND-06 (DELAY_SECONDS rate limiting): already tested in test_phase1.py::test_py02_delay_seconds — do NOT duplicate
- assertIn structural checks (not behavioral): check that the command file CONTAINS required strings/sections
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test_phase2.py — RED contract tests for HND-01 through HND-12</name>
  <files>tests/test_phase2.py</files>
  <behavior>
    - test_hnd01_file_exists: HOUND_CMD path exists check — FAILS RED (file not yet written)
    - test_hnd01_arguments_intake: command contains "$ARGUMENTS" — FAILS RED
    - test_hnd02_safelist_load: command contains "safelist-" — FAILS RED
    - test_hnd03_missing_variants_guard: command contains "/trademark-cat" routing message — FAILS RED
    - test_hnd04_serp_script_check_reuse: command contains "hound-SERP-" — FAILS RED
    - test_hnd04_token_substitution: command contains "[INSERT API KEY]" — FAILS RED (the template token referenced by name)
    - test_hnd05_bash_execution: command contains "python3" and "hound-SERP-" — FAILS RED
    - test_hnd07_safelist_filter: command contains "safelist" and "filter" (or "exclude" or "discard") — FAILS RED
    - test_hnd08_webfetch_investigation: command contains "WebFetch" — FAILS RED
    - test_hnd09_informational_exclusion: command contains "wikipedia" — FAILS RED (known exclusion domain listed)
    - test_hnd10_scoring_matrix: command contains all 8 factor names (at minimum: "Mark Criticality", "Similarity", "Geography") — FAILS RED
    - test_hnd11_risk_tiers: command contains "High" and "Medium" and "Low" and tier thresholds — FAILS RED
    - test_hnd12_low_risk_dropped: command contains language about dropping/excluding Low leads — FAILS RED
    - test_hnd_scored_output: command contains "hound_scored-" (intermediate output file name) — FAILS RED
  </behavior>
  <action>
Create tests/test_phase2.py following the exact same structure as tests/test_phase1.py.

File header docstring must state:
- "Phase 2 test suite — Trademark Hound Core"
- "Run: python3 tests/test_phase2.py"
- Tests verify structural correctness of .claude/commands/trademark-hound.md (HND-01 through HND-12)
- HND-06 is covered by test_phase1.py — not repeated here
- "These are contract tests, not behavioral tests"

Use ROOT = Path(__file__).parent.parent

Single test class: TestTrademarkHoundCommand
  HOUND_CMD = ROOT / ".claude" / "commands" / "trademark-hound.md"

  Helper _read(self): asserts file exists, returns text — same pattern as Phase 1

Tests to implement (method names and assertions):

  test_hnd01_file_exists
    self.assertTrue(self.HOUND_CMD.exists(), f"Missing: {self.HOUND_CMD}")

  test_hnd01_arguments_intake
    assertIn("$ARGUMENTS", content) — command parses inline args like /trademark-cat does

  test_hnd02_safelist_load
    assertIn("safelist-", content) — command references safelist file naming pattern

  test_hnd03_missing_variants_halt
    assertIn("/trademark-cat", content) — halt message routes to /trademark-cat
    assertIn("variants-", content) — references variants file pattern

  test_hnd04_serp_script_reuse
    assertIn("hound-SERP-", content) — references per-trademark SERP script name

  test_hnd04_token_substitution
    assertIn("[INSERT API KEY]", content) — documents the placeholder token being replaced

  test_hnd05_bash_execution
    assertIn("python3", content) — Bash tool execution of SERP script
    assertIn("hound_leads-", content) — references leads output file

  test_hnd07_safelist_filtering
    content_lower = content.lower()
    assertTrue("filter" in content_lower or "exclude" in content_lower or "discard" in content_lower)
    — safelist filtering step exists

  test_hnd08_webfetch_investigation
    assertIn("WebFetch", content) — agentic browsing tool explicitly named
    assertIn("Commerciality", content) or assertIn("COMMERCIALITY", content) — assessment dimensions named

  test_hnd09_informational_exclusion
    assertIn("wikipedia", content.lower()) — known exclusion domain listed

  test_hnd10_eight_factor_matrix
    for factor in ["Mark Criticality", "Similarity", "Geography"]:
        assertIn(factor, content) — scoring table present in command

  test_hnd11_risk_tiers
    assertIn("High", content)
    assertIn("Medium", content)
    assertIn("Low", content)
    assertTrue("15" in content or ">= 15" in content or "≥ 15" in content) — High threshold present

  test_hnd12_low_risk_dropped
    content_lower = content.lower()
    assertTrue("low" in content_lower and ("drop" in content_lower or "exclude" in content_lower or "below" in content_lower))

  test_hnd_scored_output_file
    assertIn("hound_scored-", content) — intermediate scored output file referenced

Do NOT import ast (no Python syntax check needed — the command is Markdown).
Do NOT add any test for HND-06 (DELAY_SECONDS already tested in test_phase1.py).
Use verbosity=2 in the __main__ block.
  </action>
  <verify>
    <automated>cd "/Users/woff/Trademark Hound:Cat" && python3 tests/test_phase2.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    - tests/test_phase2.py exists and runs without import errors or syntax errors
    - All tests FAIL with "Missing: .../trademark-hound.md" or AssertionError (not Python errors)
    - python3 tests/test_phase1.py still passes 100% GREEN
    - Output ends with something like "FAILED (failures=14)" or equivalent
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit RED tests and verify both suites run cleanly</name>
  <files>tests/test_phase2.py</files>
  <action>
Run the full dual-suite verification to confirm:
1. test_phase1.py is GREEN (no regressions from adding test_phase2.py)
2. test_phase2.py is RED (all tests fail due to missing command file, not due to Python errors)

Run:
  python3 tests/test_phase1.py && echo "Phase 1: GREEN" || echo "Phase 1: BROKEN"
  python3 tests/test_phase2.py 2>&1 | grep -E "^(FAIL|ERROR|OK|Ran)" || python3 tests/test_phase2.py 2>&1 | tail -5

If test_phase1.py is broken: fix before committing.
If test_phase2.py has Python import errors or AttributeError (not AssertionError): fix before committing.

Confirm: the RED failures are test failures (FAIL), not execution errors (ERROR). ERROR means a bug in the test code itself. Fix any ERRORs.

Do NOT fix the failures by creating trademark-hound.md — that is Plan 02's job.
  </action>
  <verify>
    <automated>cd "/Users/woff/Trademark Hound:Cat" && python3 tests/test_phase1.py 2>&1 | tail -1 && python3 tests/test_phase2.py 2>&1 | grep -c "^FAIL:" </automated>
  </verify>
  <done>
    - python3 tests/test_phase1.py exits 0 ("OK")
    - python3 tests/test_phase2.py exits non-zero with only FAIL lines (no ERROR lines)
    - FAIL count matches the number of test methods written (expected: ~14)
  </done>
</task>

</tasks>

<verification>
Run both suites:
  python3 tests/test_phase1.py && python3 tests/test_phase2.py 2>&1 | tail -3

Expected: Phase 1 OK, Phase 2 FAILED (all failures, no errors).
</verification>

<success_criteria>
- tests/test_phase2.py exists with ~14 test methods across TestTrademarkHoundCommand
- Every test references a real structural requirement (HND-01 through HND-12, minus HND-06)
- All tests fail RED (file does not exist yet)
- test_phase1.py remains GREEN
- No Python import errors or ERRORs in test_phase2.py output
</success_criteria>

<output>
After completion, create .planning/phases/02-trademark-hound-core/02-01-SUMMARY.md
</output>
