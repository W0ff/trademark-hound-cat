---
phase: 03-reports-safe-list-and-pipeline-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .claude/commands/trademark-report.md
  - .claude/commands/trademark-hound.md
autonomous: true
requirements:
  - HND-13
  - HND-14
  - HND-15
  - HND-16

must_haves:
  truths:
    - "trademark-report.md Step 3A markdown template includes an Attorney Review Table with THREAT? as the final column"
    - "trademark-report.md Step 3A includes a disclaimer blockquote before the Executive Summary stating the report is for attorney review and does not constitute legal advice"
    - "trademark-report.md Step 3B (CSV path) includes a disclaimer comment row or header"
    - "trademark-report.md Step 4 safelist write uses Write-to-.tmp then Bash mv (atomic pattern)"
    - "trademark-hound.md Step 7 ends with a run summary block showing funnel counts (raw leads, safelist-excluded, domain-triaged, attorney-skipped, content-triaged, scored, High/Medium/Low)"
    - "python3 tests/test_phase3.py passes TestReportFormat, TestThreatColumn, TestDisclaimer, TestRunSummary tests"
  artifacts:
    - path: ".claude/commands/trademark-report.md"
      provides: "Patched report command with THREAT? column, disclaimer, atomic safelist write"
    - path: ".claude/commands/trademark-hound.md"
      provides: "Patched hound command with run summary block at end of Step 7"
  key_links:
    - from: ".claude/commands/trademark-report.md"
      to: "safelist-[sanitized_name].json.tmp"
      via: "Write tool to .tmp then Bash mv"
      pattern: "\\.tmp.*mv"
    - from: ".claude/commands/trademark-hound.md"
      to: "Run Summary block"
      via: "In-conversation output after hound_scored write"
      pattern: "Run Summary"
---

<objective>
Patch /trademark-report and /trademark-hound to satisfy HND-13 through HND-16. These are targeted in-place edits to two existing slash command files. No new files are created.

Purpose: Turn the RED tests from Plan 01 GREEN for the report format and run summary requirements.
Output: Updated trademark-report.md (THREAT? column, disclaimer, atomic write in Step 4) and updated trademark-hound.md (run summary at end of Step 7).
</objective>

<execution_context>
@/Users/woff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/woff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-reports-safe-list-and-pipeline-validation/03-RESEARCH.md
@.planning/phases/03-reports-safe-list-and-pipeline-validation/03-01-SUMMARY.md

<interfaces>
<!-- Current trademark-report.md structure summary -->
<!--
Step 3A writes a markdown report with this section order:
  # Trademark Monitoring Report: [TRADEMARK]
  ## Executive Summary (table + narrative)
  ## Priority Action Table
  ## Scored Lead Cards
  ## Methodology Notes

Step 3B writes CSV (no disclaimer currently).

Step 4 does interactive safelist dismiss:
  - Reads current safelist JSON
  - Merges new URLs (no duplicates)
  - Writes back with Write tool (DIRECT WRITE — not atomic)
  - Reports count added and total

Current Step 4 write pattern (NOT atomic):
  "Write back to safelist-[sanitized_name].json"

Target atomic pattern:
  "Write to safelist-[sanitized_name].json.tmp using the Write tool, then run: mv safelist-[sanitized_name].json.tmp safelist-[sanitized_name].json using the Bash tool."
-->

<!-- Current trademark-hound.md structure summary -->
<!--
Step 7 ends with this report block:
  "[N] leads scored — High: [X], Medium: [Y], Low: [Z] (dropped)
  Scored leads written to: hound_scored-[sanitized_name].json"

Then this closing note:
  "This completes the Trademark Hound investigation phase. Run /trademark-hound again with a reviewed report path to update the safelist (Phase 3)."

Run summary (HND-16) must be inserted BEFORE this closing note, after the scored JSON is written.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add THREAT? column and disclaimer to /trademark-report Step 3A and Step 3B</name>
  <files>.claude/commands/trademark-report.md</files>
  <action>
    Make three edits to trademark-report.md:

    EDIT 1 — Add disclaimer to Step 3A (markdown report):
    In the Step 3A report template, immediately after the opening header block (after the `---` separator following "Prepared by: Trademark Hound"), insert:

    ```markdown
    > **DISCLAIMER:** This report is prepared for attorney review and internal use only. It does not constitute legal advice, an opinion of counsel, or a legal conclusion. All findings require independent legal evaluation before any action is taken.

    ---
    ```

    This places the disclaimer between the header block and the Executive Summary, making it the first thing an attorney reads.

    EDIT 2 — Add Attorney Review Table to Step 3A (HND-14):
    In the Step 3A template, insert a new section between the Executive Summary and the Priority Action Table. The new section is:

    ```markdown
    ## Attorney Review Table

    | Date | Trademark/Variant | Entity Name | URL | Industry | Risk Score | Risk Tier | Infringement Analysis | THREAT? |
    |------|-------------------|-------------|-----|----------|------------|-----------|----------------------|---------|
    [Sort leads by total_score descending. For each lead, output one row:]
    | [report_date] | [variant] | [entity_name] | [url] | [industry] | [total_score] | [risk_tier] | [One sentence summary of the strongest infringement factor and evidence from the scoring.] | |

    *THREAT? column: Attorney marks YES (enforce), NO (dismiss to safelist), or leave blank (needs further review). Run `/trademark-hound [TRADEMARK] [this-report-filename]` after filling in this column to update the safelist.*

    ---
    ```

    The THREAT? column is blank (empty) in fresh reports — do not pre-fill it.

    EDIT 3 — Add disclaimer to Step 3B (CSV report):
    In Step 3B, before the "Write a CSV file" instruction, add:

    "The first row of the CSV file must be a comment row: `# DISCLAIMER: For attorney review only. Does not constitute legal advice.` — prefix the hash-comment as the literal first line before the header row."

    Also update the CSV formatting rules to state: "First line: the disclaimer comment row starting with `#`. Second line: the column headers. Subsequent lines: data rows."

    After all edits, read the updated file and confirm:
    - "THREAT?" appears in the file
    - "does not constitute legal advice" appears in the file
    - "Attorney Review Table" appears in the file
  </action>
  <verify>
    <automated>python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase3.py" TestThreatColumn TestDisclaimer 2>&1 | tail -5</automated>
  </verify>
  <done>
    TestThreatColumn and TestDisclaimer test classes pass. trademark-report.md contains THREAT? column, Attorney Review Table section, and disclaimer text in both the 3A markdown template and 3B CSV section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Step 4 safelist write to atomic pattern + add run summary to /trademark-hound</name>
  <files>.claude/commands/trademark-report.md, .claude/commands/trademark-hound.md</files>
  <action>
    EDIT A — Atomic write in trademark-report.md Step 4 (HND-18):
    Find the safelist write instruction in Step 4. It currently says to write back directly to "safelist-[sanitized_name].json". Replace the write instruction with the atomic pattern:

    Replace the existing write sentence with:
    "Before writing, check whether `safelist-[sanitized_name].json.tmp` exists using the Bash tool (`ls safelist-[sanitized_name].json.tmp 2>/dev/null`). If it exists, remove it first (`rm safelist-[sanitized_name].json.tmp`). Then write the merged safelist array to `safelist-[sanitized_name].json.tmp` using the Write tool. Then run `mv safelist-[sanitized_name].json.tmp safelist-[sanitized_name].json` using the Bash tool to atomically replace the live file."

    The report count line after the write ("Safelist updated: [N] URLs added. `safelist-[sanitized_name].json` now contains [M] total entries.") is already present and satisfies HND-19 for the interactive path — leave it unchanged.

    EDIT B — Run summary in trademark-hound.md Step 7 (HND-16):
    Find the closing section of Step 7. It currently ends with:
    "[N] leads scored — High: [X], Medium: [Y], Low: [Z] (dropped)\nScored leads written to: hound_scored-[sanitized_name].json"

    After that line (but before the CRITICAL note at the bottom), insert the run summary block:

    "After writing hound_scored-[sanitized_name].json, display the following run summary block in the conversation:

    ```
    === Trademark Hound Run Summary ===
    Trademark: [TRADEMARK]
    Run date: [today's date in YYYY-MM-DD]

      Raw SERP leads:              [N from Step 4 post-SERP count]
      Excluded by safelist:        [M from Step 5]
      Excluded by domain triage:   [Stage 1 count from Step 6]
      Skipped by attorney:         [Stage 1.5 count from Step 6]
      Excluded by content triage:  [Stage 2 count from Step 6]
      Scored leads (Med + High):   [count_high + count_medium]

      High (>= 15):                [count_high]
      Medium (10-14):              [count_medium]
      Low (< 10, dropped):         [count_low — calculate as investigated_count minus count_high minus count_medium]

    Next step: Run `/trademark-report [TRADEMARK]` to generate the attorney report.
    ```

    Track count_low as a running counter during the scoring loop: increment it each time a lead scores below 10 and is dropped."

    After both edits, verify:
    - ".tmp" appears in trademark-report.md
    - "mv" appears in trademark-report.md
    - "Run Summary" appears in trademark-hound.md
  </action>
  <verify>
    <automated>python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase3.py" TestAtomicWrite TestRunSummary 2>&1 | tail -5</automated>
  </verify>
  <done>
    TestAtomicWrite and TestRunSummary test classes pass. trademark-report.md Step 4 uses .tmp + mv atomic write pattern. trademark-hound.md Step 7 includes a run summary block with all funnel counts. Full test suite still passes for Phase 1 and 2 tests.
  </done>
</task>

</tasks>

<verification>
python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase3.py" 2>&1 | tail -10

Expected: TestReportFormat, TestThreatColumn, TestDisclaimer, TestRunSummary, TestAtomicWrite all GREEN.
TestSafelistIngestion and TestSafelistCountReport will remain RED (those are Plan 03).

Also run full suite to confirm no Phase 1/2 regressions:
python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase1.py" && python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase2.py" 2>&1 | tail -3
</verification>

<success_criteria>
HND-13 through HND-16 are satisfied: report has THREAT? column table (HND-14), disclaimer in markdown and CSV paths (HND-15), report filename pattern unchanged (HND-13), run summary shown at end of hound pipeline (HND-16). Step 4 safelist write is atomic (HND-18 partially covered for interactive path). Tests for these requirements are GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reports-safe-list-and-pipeline-validation/03-02-SUMMARY.md`
</output>
