---
phase: 03-reports-safe-list-and-pipeline-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - .claude/commands/trademark-hound.md
autonomous: true
requirements:
  - HND-17
  - HND-18
  - HND-19

must_haves:
  truths:
    - "Attorney can run /trademark-hound TESTMARK HOUND_REPORT_TESTMARK_2026-04-04.md to trigger safelist ingestion — a second argument matching the HOUND_REPORT_*.md pattern triggers the re-invocation branch"
    - "Re-invocation branch reads the THREAT? column in the Attorney Review Table, collects all URLs where THREAT? is exactly NO (case-insensitive), and adds them to the safelist"
    - "Re-invocation branch uses atomic write: Write to safelist-[TRADEMARK].json.tmp then Bash mv"
    - "Re-invocation branch reports: X entries marked NO (added to safelist), Y entries marked YES (retained), Z entries blank/unreviewed (skipped)"
    - "Blank THREAT? entries are NOT added to safelist — the branch explicitly skips blanks, dashes, and any value other than NO"
    - "Duplicate URLs already in safelist are silently skipped (no double-counting)"
    - "python3 tests/test_phase3.py passes all test classes including TestSafelistIngestion and TestSafelistCountReport"
  artifacts:
    - path: ".claude/commands/trademark-hound.md"
      provides: "Second-mode intake branch for reviewed report path + safelist ingestion"
  key_links:
    - from: ".claude/commands/trademark-hound.md (Intake section)"
      to: "HOUND_REPORT_*.md (reviewed report)"
      via: "Second argument detection in Intake section"
      pattern: "HOUND_REPORT_"
    - from: ".claude/commands/trademark-hound.md (safelist ingestion step)"
      to: "safelist-[sanitized_name].json.tmp"
      via: "Write tool to .tmp then Bash mv"
      pattern: "\\.tmp.*mv"
---

<objective>
Add the re-invocation branch to /trademark-hound. When called with a second argument that is a reviewed report file path (matching HOUND_REPORT_*.md), hound switches to safelist-ingestion mode: reads the THREAT? column, atomically writes NO-marked URLs to the safelist, and reports the outcome.

Purpose: Closes the HND-17 feedback loop — the full pipeline now cycles: Cat → Hound → Report (attorney reviews THREAT? column) → Hound re-invoked with report path → safelist updated → future Hound runs suppress known false positives.
Output: Updated trademark-hound.md with a complete second-mode intake section.
</objective>

<execution_context>
@/Users/woff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/woff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-reports-safe-list-and-pipeline-validation/03-RESEARCH.md
@.planning/phases/03-reports-safe-list-and-pipeline-validation/03-02-SUMMARY.md

<interfaces>
<!-- Key edge cases from research — these MUST be addressed in the command text -->
<!--
- THREAT? blank = unreviewed → skip (do NOT add to safelist)
- THREAT? = "YES" → keep for enforcement, do NOT add to safelist
- THREAT? = "NO" → add URL to safelist
- Duplicate URL already in safelist → skip silently, do not increment add count
- Report file not found → halt with clear error message
- safelist-[TRADEMARK].json.tmp orphan check → rm if exists before writing
- Second argument detection: only fire re-invocation branch when 2nd arg matches HOUND_REPORT_*.md pattern OR that exact file exists — do not auto-detect from trademark name alone
-->

<!-- Atomic write pattern (established in Plan 02 for trademark-report.md Step 4) -->
<!--
1. Check for orphaned .tmp: ls safelist-[sanitized_name].json.tmp — remove if found
2. Write merged array to safelist-[sanitized_name].json.tmp with Write tool
3. mv safelist-[sanitized_name].json.tmp safelist-[sanitized_name].json with Bash tool
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add re-invocation branch to /trademark-hound Intake section</name>
  <files>.claude/commands/trademark-hound.md</files>
  <action>
    Edit trademark-hound.md to add the second-mode intake branch. Make two edits:

    EDIT 1 — Update the Intake section to detect a second argument:
    After the existing `$ARGUMENTS` parsing logic (which extracts trademark name and optional goods/services), add a second-argument check BEFORE asking for goods/services:

    "After extracting the trademark name (first token), check whether a second argument is present and matches the pattern `HOUND_REPORT_*.md` (the filename starts with `HOUND_REPORT_` and ends with `.md`), OR the second token is a path to an existing file with that naming pattern.

    If a second argument matching this pattern is detected:
      - Store the report path as `reviewed_report_path`
      - Set `mode = 'safelist_ingestion'`
      - Do NOT ask for goods/services description
      - Skip directly to the Safelist Ingestion step below
      - Do NOT run SERP search, content triage, or scoring

    If no second argument (or second argument does not match HOUND_REPORT_*.md pattern):
      - Set `mode = 'investigate'`
      - Continue with normal intake (ask for goods/services if not provided)
      - Proceed through Steps 1–7 as before"

    EDIT 2 — Add the Safelist Ingestion step as a new named section before the CRITICAL footer note:
    Insert the following section at the end of the command file, after Step 7 and before the CRITICAL note:

    ---

    ## Safelist Ingestion (re-invocation mode only)

    *Execute this section only when `mode = 'safelist_ingestion'`. Skip entirely in normal investigation mode.*

    **Step SI-1: Verify report file exists**

    Use the Read tool to check whether `[reviewed_report_path]` exists.

    If it does NOT exist, output:
    > "Report file not found: [reviewed_report_path]. Check the filename and try again."
    Then stop.

    Sanitize the trademark name for filenames (same rule as normal intake). Store as `sanitized_name`.

    **Step SI-2: Load current safelist**

    Check whether `safelist-[sanitized_name].json` exists.
    - If yes: read it, parse as JSON array, store as `current_safelist`
    - If no: set `current_safelist = []`

    **Step SI-3: Parse THREAT? column from Attorney Review Table**

    Read `[reviewed_report_path]`. Locate the "## Attorney Review Table" section.

    For each data row in that table:
    - Extract the THREAT? column value (last column). Trim whitespace. Compare case-insensitively.
    - If THREAT? == "NO": collect the URL from the URL column of that row
    - If THREAT? == "YES": note this URL as retained (count for summary)
    - If THREAT? is blank, "-", "?", or any other value: skip this row (count as unreviewed)

    Track:
    - `urls_to_add` = list of URLs where THREAT? == "NO" that are NOT already in `current_safelist`
    - `count_yes` = number of YES rows
    - `count_blank` = number of blank/unreviewed rows
    - `count_already_in_safelist` = URLs where THREAT? == "NO" but URL is already in `current_safelist` (skip silently, do not re-add)

    **Step SI-4: Atomic safelist write**

    Merge `current_safelist` + `urls_to_add` (no duplicates — already filtered in SI-3).

    Check for orphaned tmp file: use the Bash tool to run `ls safelist-[sanitized_name].json.tmp 2>/dev/null`. If it exists, run `rm safelist-[sanitized_name].json.tmp`.

    Write the merged array to `safelist-[sanitized_name].json.tmp` using the Write tool.
    Then run `mv safelist-[sanitized_name].json.tmp safelist-[sanitized_name].json` using the Bash tool.

    **Step SI-5: Report outcome (HND-19)**

    Output:
    > "=== Safelist Update Complete ===
    > Trademark: [TRADEMARK]
    > Report reviewed: [reviewed_report_path]
    >
    >   Entries marked NO (added to safelist):  [len(urls_to_add)]
    >   Already in safelist (skipped):          [count_already_in_safelist]
    >   Entries marked YES (retained):          [count_yes]
    >   Entries blank / not yet reviewed:       [count_blank]
    >
    >   safelist-[sanitized_name].json now contains [len(merged)] total entries.
    >   These URLs will be excluded from all future /trademark-hound runs."

    ---

    After writing the edits, verify:
    - "HOUND_REPORT_" appears in the Intake section (second-argument detection)
    - "Safelist Ingestion" appears in the file (new section header)
    - ".tmp" appears at least twice in the file (Step 4 atomic and SI-4 atomic)
    - "mv" appears in the file
    - "entries added" or "total entries" appears in the file (SI-5 count reporting)
    - "blank" and "unreviewed" appears in the file (blank THREAT? guard)
  </action>
  <verify>
    <automated>python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase3.py" 2>&1 | tail -5</automated>
  </verify>
  <done>
    All Phase 3 tests are GREEN. trademark-hound.md contains: second-mode intake branch detecting HOUND_REPORT_*.md second argument, Safelist Ingestion section with SI-1 through SI-5, atomic write pattern (.tmp + mv), blank-THREAT?-guard, and count reporting. Full test suite (phases 1, 2, 3) passes with no regressions.
  </done>
</task>

</tasks>

<verification>
Full suite:
python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase1.py" && python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase2.py" && python3 "/Users/woff/Trademark Hound:Cat/tests/test_phase3.py" 2>&1 | tail -5

Expected: OK — all tests pass across all three phases.
</verification>

<success_criteria>
HND-17, HND-18, HND-19 are satisfied: /trademark-hound has a re-invocation branch (HND-17), both the re-invocation branch and the interactive Step 4 path use atomic tmp+mv writes (HND-18), and the re-invocation branch reports a full accounting of added/retained/blank/total entries (HND-19). All Phase 3 tests are GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reports-safe-list-and-pipeline-validation/03-03-SUMMARY.md`
</output>
